# Self Playtest

当没有真实玩家反馈且游戏可运行时，self-playtest 是主要输入来源。它不等同于真实玩家反馈。

## 最新结果

2026-05-07 阶段条件进度复核。这不是真实玩家反馈；触发信号来自 v0.2 内容弧线的 3-15 分钟检查。

- 反馈状态：Issue #1/#2 没有新的玩家补充，本轮不重复回复。
- 模拟方式：每秒点击一次，并在可购买时优先购买当前可负担升级；只使用当前 `src/game.ts` 逻辑。
- 3-15 分钟升级节奏：第 348、427、502、606、724、862、1050 秒仍出现自动采集器或调校升级。
- 第 606 秒状态：10 台自动采集器、5 次调校、每秒 +3 星尘，已经进入 `星尘引擎室`。
- Gap：升级没有停滞，但阶段条件缺少当前进度。玩家看到“拥有 10 台自动采集器并完成 4 次调校”时，需要自己对照统计和倍率推断进度。
- 切片：把现有 `下一阶段` 文案改成当前进度格式，例如 `自动采集器 6/10，调校 2/4`。
- 复杂度复核：不新增资源、按钮、面板、升级类型、存档字段、指标字段或反馈渠道。

Gap: 已用同一阶段文案行处理。下一轮若继续推进 15-60 分钟或首次回访，必须先读取 local-only metrics、做回访复核，或等待真实玩家补充；不得直接新增第三种升级。

2026-05-07 引擎室后续目标复核。这不是真实玩家反馈；触发信号来自 v0.2 内容弧线的 15-60 分钟 / 首次回访检查。

- 现状：`星尘引擎室` 已经代表 v0.2 的第一条长期生产线，且游戏已有离线收益提示。
- 15-60 分钟风险：达到 `星尘引擎室` 后，下一步文案是“v0.2 阶段目标已达成：继续观察 15 分钟后的回访节奏”，这更像 operator 备注，不像玩家能执行的目标。
- 切片：只替换 `星尘引擎室` 的 `nextRequirement`，提示玩家“离开一会儿再回来，查看引擎室积累的离线星尘”。
- 前 60 秒护栏：不影响初始目标、购买进度、自动采集器、调校工具、采集反馈或购买反馈。
- 复杂度复核：不新增资源、按钮、面板、升级类型、存档字段、指标字段或反馈渠道。

Gap: 已用现有阶段行文案处理。后续不得直接新增第三或第四种升级；如果首次回访仍缺少目标，先读取 local-only session history 或等待真实玩家补充。

2026-05-07 v0.2 阶段完成反馈复核。这不是真实玩家反馈；触发信号来自用户手动反馈和 v0.2 内容弧线检查。

- 现状：工坊阶段已经显示阶段名、阶段说明和下一阶段条件，最近升级进度也能指向自动采集器或调校工具。
- 3-15 分钟风险：购买导致阶段跨越时，玩家只会看到阶段行文字变化；购买瞬间的反馈仍是普通“每秒星尘”或“调校完成”，弱化了阶段推进的存在感。
- 切片：复用现有事件反馈区。当购买前后的 `getWorkshopStage` 阶段名不同时，显示“工坊升级：阶段名 · 阶段说明”。
- 前 60 秒护栏：未跨阶段的购买仍显示原购买反馈；第一台、第二台自动采集器不会伪造阶段升级。
- 复杂度复核：不新增资源、按钮、面板、升级类型、存档字段、指标字段或反馈渠道。

Gap: 已用同一事件反馈区替换处理。后续不得直接新增第三或第四种升级；下一轮必须来自真实反馈、self-playtest gap、local-only metrics 或 research-backed 决策。

2026-05-07 v0.2 最近升级进度复核。这不是真实玩家反馈；触发信号来自用户手动反馈和 v0.2 内容弧线检查。

- 现状：目标提示已经会在调校出现后说“扩建或调校”，但进度条仍固定显示下一台自动采集器的购买进度。
- 3-15 分钟风险：当调校工具成本低于下一台自动采集器时，玩家看到的进度会指向较远目标，削弱“一个升级就在眼前”的感受。
- 切片：把进度区改为“下一升级进度”，在自动采集器和调校工具之间选择当前成本更近的一项。
- 前 60 秒护栏：0 台自动采集器时仍只指向第一台自动采集器，不改变第一目标、经济、资源、按钮或面板。
- 文档：新增 `docs/CONTENT_ARC.md`，先定义 0-60 分钟、3-15 分钟、15-60 分钟和首次回访内容弧线，避免后续无边界扩系统。

Gap: 已用同一进度区替换处理。后续不得直接新增第三或第四种升级；下一轮必须来自真实反馈、self-playtest gap、local-only metrics 或 research-backed 决策。

2026-05-07 v0.2 工坊阶段切片复核。这不是真实玩家反馈；触发信号来自用户手动反馈。

- 反馈状态：用户明确表示当前玩法仍然干枯太少，并确认继续推进 v0.2。
- 设计选择：使用现有 `autoCollectors` 和 `autoCollectorEfficiencyLevel` 推导工坊阶段，不新增第二资源、按钮、面板、任务系统或 prestige。
- 初始阶段：`火花工作台`，提示下一阶段需要 3 台自动采集器。
- 早期阶段：3 台自动采集器后进入 `星尘小间`，提示下一阶段需要 2 次调校。
- 中段阶段：5 台自动采集器且 2 次调校后进入 `稳定工坊`，提示下一阶段需要 10 台自动采集器和 4 次调校。
- v0.2 阶段目标：10 台自动采集器且 4 次调校后进入 `星尘引擎室`，提示继续观察 15 分钟后的回访节奏。
- 复杂度复核：primary resource 仍为 1；secondary resources 仍为 0；upgrade types 仍为 2；visible panels 未增加；feedback channel 未增加；save version 未改变。

Gap: 本轮处理的是内容弧线可见性，不证明 15 分钟后留存已经解决。后续若继续反馈“内容少”，优先评估是否需要第 4 种升级类型，而不是直接加第二资源或多面板。

2026-05-07 3-5 分钟调校可见性复核。这不是真实玩家反馈。

- 反馈状态：Issue #1 和 Issue #2 都没有玩家新补充，只有已记录的一次 operator 回复，本轮不重复回复 issue。
- 模拟方式：每秒点击一次，并在可购买时购买当前更便宜的自动采集器或调校工具；只使用现有 `src/game.ts` 逻辑。
- 第 180 秒状态：6 台自动采集器、调校等级 2、调校倍率 x1.2、每秒 +1.44 星尘，下一台自动采集器成本 114，下一次调校成本 81。
- 第 240 秒状态：7 台自动采集器、调校等级 3、调校倍率 x1.3、每秒 +1.82 星尘，下一台自动采集器成本 171，下一次调校成本 146。
- 第 300 秒状态：7 台自动采集器、调校等级 4、调校倍率 x1.4、每秒 +1.96 星尘，下一台自动采集器成本 171，下一次调校成本 263。
- UI gap：3-5 分钟时调校工具已经成为主要成长路径之一，但主屏统计仍显示恒定的“点击收益 1”，没有展示调校结果；这会削弱“扩建或调校”目标提示的可验证性。
- 切片：将同一统计格从“点击收益”替换为“调校倍率”，显示当前自动采集效率倍率；不新增面板、按钮、资源、升级类型或指标字段。

Gap: 已用同一统计格替换处理。后续仍不应新增第三种升级或第二资源；若该阶段继续 no-change，应进入 stage review、metrics readback 或定义内容弧线。

2026-05-07 60-300 秒升级节奏复核。这不是真实玩家反馈。

- 反馈状态：Issue #1 和 Issue #2 都没有玩家新补充。
- 模拟方式：每秒点击一次，并在可购买时购买自动采集器或调校工具；`auto-first`、`tune-first` 和 `cheapest` 三种策略在当前数值下结果一致。
- 升级事件：第 10、23、39、55、76、100、126、160、194、238、290 秒。
- 第 300 秒状态：7 台自动采集器、调校等级 4、每秒 +1.96 星尘。
- 第 300 秒距离下一次升级约 47 秒；最近一次升级发生在第 290 秒。
- 结论：当前 60-300 秒仍能稳定出现“再一个升级在眼前”的事件，不支持为了持续运营直接新增第三种升级、第二资源或新面板。

Gap: 本轮没有足够具体的玩法缺口。继续等待真实玩家补充、local-only metrics 可解释问题，或新的 self-playtest 证据。

2026-05-07 Issue #2 发布后文案预算和回访提示复核。这不是真实玩家反馈。

- 反馈状态：Issue #1 和 Issue #2 都没有玩家新补充，只有已记录的一次 operator 回复。
- 目标提示文案计数：初始状态 18 个中文字符；已有自动采集器但无调校时 25 个；已有调校时 18 个。
- 首屏可见中文字符计数：初始状态 107；已有自动采集器后 115；已有调校后 108，仍低于 300 中文字符预算。
- 窄屏布局复核：`.action-row` 仍有 `flex-wrap: wrap`，560px 以下仍纵向排列；按钮仍未设置 `white-space: nowrap`。
- 回访提示复核发现：极小离线收益会被 `formatNumber` 显示为 `0`，导致“离线获得 0 星尘”。
- 修复：离线收益低于 `0.1` 时不展示提示，也不记录 `offline_reward_claimed`，避免把未实际展示的 0 值提示记入本地指标。

Gap: 本轮未触发 `SIMPLIFY`，也没有证据支持新增玩法。后续继续等待新反馈；若出现新的 post-60s 具体位置，再进入 `OPERATE`。

2026-05-07 Issue #2 60 秒后目标提示切片。这不是真实留存指标，只是用真实反馈触发的本地复核。

- 反馈：Issue #2 表示“只能玩前60秒，然后就无聊了”，希望有更丰富的游戏内容。
- 经济复核：每秒点击一次并优先购买可负担升级时，第 55 秒购买第 1 个调校工具，第 76 秒第 4 台自动采集器，第 100 秒第 2 次调校，第 126 秒和第 160 秒继续购买自动采集器。
- 判断：60 秒后已有可达升级事件，但目标提示仍主要像是在追下一台自动采集器，没有充分表达“扩建或调校”的已有选择。
- 切片：只调整目标提示文案；0 台自动采集器仍提示第一个自动采集器，有自动采集器但无调校时提示“下一台自动采集器或第一次调校”，已有调校后提示“扩建或调校”。
- 复杂度复核：不新增资源、升级类型、按钮、面板、指标字段或反馈渠道。

Gap: 该切片只能改善目标表达，不能证明长期内容深度已经足够。发布后若 Issue #2 有新补充，再决定是否继续 research 更深但仍低复杂度的内容。

2026-05-06 本地/公开预览检查。这不是真实玩家反馈。

- 前 10 秒：玩家能立即看到“星尘”和主要“采集”动作。
- 前 30 秒：第一个升级可见，但按钮文案只显示 `10`，没有明确说明这是星尘成本。
- 前 60 秒：购买第一台自动采集器后，玩家能通过“每秒 +0.2”看到被动增长。

Gap: 第一个升级成本文案应更清楚地说明单位和意图。

2026-05-06 后续 self-playtest。这不是真实玩家反馈。

- 前 10 秒：主要点击动作仍然清楚。
- 前 30 秒：上一次修复后，成本文案更清楚。
- 前 60 秒：玩家能从按钮和进度条推断第一个目标，但没有一句话直接说明“攒够星尘购买第一台自动采集器”。

Gap: 第一个目标应由一句短文案直接说明。

2026-05-06 研究支撑的后续检查。这不是真实玩家反馈。

- 前 10 秒：首目标文案给玩家直接目标。
- 前 30 秒：升级成本和进度条可读。
- 前 60 秒：在引入任何新系统前，早期 generator 进度仍需要可见里程碑支撑。

Gap: 在不增加奖励的前提下，展示早期自动采集器小里程碑进度。

2026-05-06 本地指标辅助模拟。这不是真实玩家反馈。

- 模拟 60 秒内每秒点击一次。
- 第 10 秒购买第一台自动采集器。
- 第 23 秒购买第二台自动采集器。
- 第 39 秒购买第三台自动采集器。

Gap: 固定 `0 / 2` 里程碑会在第一分钟结束前完成，之后不再提供方向。

2026-05-06 North Star 回访检查。这不是真实玩家反馈。

- Save/load 会按离开时间应用被动产出。
- UI 写着“自动保存”，但没有告诉回访玩家离开期间获得了什么。

Gap: 离线进度增加星尘时，应让玩家看见收益。

2026-05-06 离线进度后续检查。这不是真实玩家反馈。

- Save/load 现在能报告离开期间获得了多少星尘。
- 只有离线收益为正时，回访玩家会看到小型“离线获得 … 星尘”提示。

2026-05-06 本地指标辅助目标文案检查。这不是真实玩家反馈。

- 模拟：每秒点击一次时，自动采集器会在第 10、23、39 秒购入。
- 前 10 秒：首次购买前，初始目标文案有用。
- 前 30 秒：第一次购买后，目标文案仍然写“购买第一个自动采集器”。
- 前 60 秒：里程碑在 3 / 5 时仍然有用，但主目标文案已经过期。

Gap: 购买第一台自动采集器后，目标提示应该更新。

2026-05-06 购买动作反馈检查。这不是真实玩家反馈。

- 前 10 秒：玩家会攒够星尘并购买第一台自动采集器。
- 前 30 秒：购买动作会改变数字和按钮状态，但没有一句话确认“自动采集器已经启动”。
- 前 60 秒：玩家能继续追下一台自动采集器，但升级成功瞬间的反馈偏弱。

Gap: 购买自动采集器后应显示短暂、非打断式确认反馈。

2026-05-06 购买反馈布局检查。这不是真实玩家反馈。

- 前 10 秒：购买前没有事件提示，按钮和目标区域稳定。
- 前 30 秒：购买自动采集器后会插入短暂确认反馈。
- 前 60 秒：如果反馈区域没有预留空间，确认文案会推动下方控件，形成轻微布局跳动。

Gap: 反馈消息区域应提前占位，避免购买瞬间推动布局。

2026-05-06 购买进度标签检查。这不是真实玩家反馈。

- 前 10 秒：进度条显示攒够第一台自动采集器成本的进度。
- 前 30 秒：目标文案和里程碑已经分别说明“下一台自动采集器”和“自动采集器数量”。
- 前 60 秒：进度条标签“下个目标”偏泛，容易和目标文案/里程碑混淆。

Gap: 进度条标签应明确为“购买进度”。

2026-05-06 发布基础设施收尾后的 self-playtest。这不是真实玩家反馈。

- 前 10 秒：星尘读数、采集按钮、购买按钮、购买进度和首目标都可见。
- 前 30 秒：购买按钮的成本单位、购买进度和首目标共同指向第一台自动采集器。
- 前 60 秒：购买确认、动态目标、动态里程碑和离线收益提示都围绕同一个核心循环，没有新增资源或奖励系统。
- 风险：首屏已经包含资源、事件区域、双动作、进度、目标、里程碑、统计和反馈入口，继续添加提示会接近 UI 复杂度上限。

Gap: 本轮未发现足够具体的新增改动 gap；不应为了保持迭代而添加新玩法或新提示。

2026-05-06 issue form 后续 self-playtest。这不是真实玩家反馈。

- 前 10 秒：反馈入口仍然只是面板底部的单一链接，没有新增首屏说明文字。
- 前 30 秒：点击反馈才会打开 GitHub Issue Form；未点击时不打断采集和购买节奏。
- 前 60 秒：issue form 改善的是外部反馈结构，不改变星尘、自动采集器、购买进度或里程碑。
- 风险：如果继续在游戏内解释反馈表单，会把反馈获取问题推回首屏 UI 复杂度问题。

Gap: 本轮未发现足够具体的代码改动 gap；继续等待真实反馈，或后续研究如何在不打扰首屏的情况下邀请外部 playtest。

2026-05-06 经济节奏回访模拟。这不是真实玩家反馈。

- 模拟方式：使用当前 `src/game.ts` 逻辑，每秒点击一次，可购买自动采集器时立即购买。
- 前 10 秒：第 10 秒购买第一台自动采集器，之后购买进度回到 0%，但目标和确认反馈仍解释状态变化。
- 前 30 秒：第 23 秒购买第二台自动采集器；第 30 秒时有 2 台自动采集器、每秒 +0.4 星尘，下一台购买进度约 45%。
- 前 60 秒：第 39 秒购买第三台自动采集器；第 60 秒时有 3 台自动采集器、每秒 +0.6 星尘，下一台购买进度约 98%。
- North Star 检查：“one more upgrade is within reach” 仍成立，不需要调整经济数值。

Gap: 本轮未发现足够具体的经济或 UI 改动 gap；不要因为没有真实反馈而改数值。

2026-05-07 Issue #1 后续检查。这基于真实玩家反馈，但本检查本身不是额外真实反馈。

- 反馈信号：玩家不知道为何要采集、采集能带来什么、如何提高采集兴奋点。
- 前 10 秒：原 UI 能看到星尘和采集按钮，但动机主要靠玩家自行推断。
- 前 30 秒：购买按钮说明了成本，进度条说明了下一台自动采集器进度，但“星尘为什么值得攒”不够直接。
- 前 60 秒：自动采集器和每秒产出已经存在，适合用一条短文案把采集、购买和持续产出串起来。

Gap: 在不新增系统的前提下，首屏应直接说明星尘会转化为自动采集器和持续生产，并给采集点击一个短暂反馈。

2026-05-07 Issue #1 修复后复核。这不是真实玩家反馈。

- 前 10 秒：星尘读数、采集按钮和“星尘会变成自动采集器，让工坊持续产出”同时可见，已直接回答采集动机。
- 前 30 秒：点击采集会出现短暂反馈，说明本次星尘正在推进下一台自动采集器；购买按钮仍保留成本和单位。
- 前 60 秒：目标文案、购买进度、里程碑和购买确认都围绕同一条链路：采集星尘 → 购买自动采集器 → 提高持续产出。
- 风险：首屏已经包含资源、动机文案、事件反馈、双动作、购买进度、目标、里程碑、统计和反馈入口。继续添加提示会增加噪音，并可能重复处理同一 issue。

Gap: 本轮未发现足够具体的新改动 gap；不要继续基于 Issue #1 叠加提示或新增系统，等待新玩家信息或新的研究问题。

2026-05-07 Soft Automation 实施复核。这不是真实玩家反馈。

- 前 10 秒：玩家仍只看到星尘、采集、自动采集器和调校工具；调校工具成本为 25 星尘，且没有自动采集器前不会生效，不抢走第一台自动采集器的 10 星尘目标。
- 前 30 秒：玩家可以先买自动采集器，再理解调校工具是强化自动产出的第二选择。
- 前 60 秒：玩法仍围绕同一链路：采集星尘、购买自动采集器、提升持续产出；没有新增资源、面板或反馈渠道。
- 风险：动作区现在有 3 个按钮，需要保持换行布局，避免长文案挤压。

Gap: 本轮实现后应通过 UI 测试和构建确认布局文案可渲染；后续真实反馈前不要继续增加第三种升级。

2026-05-07 Soft Automation 60 秒模拟。这不是真实玩家反馈。

- 模拟方式：每秒点击一次，优先购买可负担的自动采集器，再购买可负担的调校工具。
- 自动采集器购买时间仍为第 10、23、39 秒，和调校工具前的经济节奏一致。
- 第 55 秒购买第 1 个调校工具；第 60 秒时有 3 台自动采集器、调校等级 1、每秒 +0.66 星尘、下一台自动采集器成本 34、下一次调校成本 45。
- 结论：调校工具没有抢走第一台自动采集器目标，也没有把第一分钟改成复杂系统；它只在玩家理解自动采集器后提供第二个低复杂度选择。

Gap: 本轮未发现需要立即调数值的证据；等待真实玩家反馈或后续本地指标再判断调校工具成本。

2026-05-07 Soft Automation 前置条件文案检查。这不是真实玩家反馈。

- 前 10 秒：调校工具在没有自动采集器时会被禁用；若仍显示“需要 25 星尘”，玩家可能误以为只要攒够星尘即可购买。
- 前 30 秒：购买第一台自动采集器后，调校工具才成为有效选择，此时显示“自动采集效率 +10% · 需要 25 星尘”更准确。
- 决策：0 台自动采集器时，调校工具文案改为“需要先购买自动采集器”；有自动采集器后再显示效率和成本。

Gap: 已修复；不改变经济数值、资源、面板或反馈渠道。

2026-05-07 Soft Automation 发布后 no-change 复核。这不是真实玩家反馈。

- 输入状态：Issue #1 仍 open，但没有玩家新补充；没有新 issue。
- 前 10 秒：玩家仍只需理解星尘、采集按钮、自动采集器目标和调校工具前置条件；第 10 秒可购买第一台自动采集器。
- 前 30 秒：第 23 秒可购买第二台自动采集器；调校工具已可见但仍不是首要目标。
- 前 60 秒：模拟在第 39 秒购买第三台自动采集器，第 55 秒购买第一项调校工具；第 60 秒时有 3 台自动采集器、调校等级 1、每秒 +0.66 星尘。
- 风险：第 55 秒购买调校工具后，下一台自动采集器成本 34，当前星尘约 8.9，下一目标距离会变远；但这是刚购买一个升级后的自然回落，暂无真实反馈说明它造成困惑。

Gap: 本轮没有足够证据支持继续新增第三种升级、改经济数值或增加首屏解释；等待真实反馈或后续本地指标再判断调校工具成本与节奏。

2026-05-07 First-screen text budget 审计。这不是真实玩家反馈。

- 输入状态：无新 issue 或玩家补充；本轮只审计复杂度预算，不改游戏。
- 文案统计方式：按当前 `src/App.tsx` 首屏可见中文文案统计 Han 字符，不把数字和标点计入中文字符。
- 初始状态：约 101 个中文字符。
- 购买第一台自动采集器后：约 102 个中文字符。
- 采集反馈短暂出现时：约 118 个中文字符。
- 购买反馈短暂出现时：约 112 个中文字符。
- 复杂度复核：primary resource 仍为 1；secondary resources 为 0；upgrade types 为 2；visible panels 未增加；反馈渠道仍为单一 GitHub Issue Form。
- 解释：动作区有 3 个按钮，但其中 2 个是升级购买控件，受 `upgrade types max 3` 约束；主玩家采集动作仍为 1。

Gap: 未触发 `SIMPLIFY`。当前首屏文案低于 300 中文字符预算，但动作区长文案仍是后续窄屏检查重点；无真实反馈前不新增提示或第三种升级。

2026-05-07 窄屏动作区布局复核。这不是真实玩家反馈。

- 输入状态：无新 issue 或玩家补充；本轮只读检查 CSS，不改游戏。
- `src/styles.css` 中 `.action-row` 已设置 `flex-wrap: wrap` 和 `align-items: stretch`。
- `.primary-action`、`.upgrade-action` 使用 `flex: 1 1 180px` 和 `min-height: 52px`，允许按钮在空间不足时换行。
- `@media (max-width: 560px)` 将 `.action-row` 改为 `flex-direction: column` 和 `align-items: stretch`，窄屏下三个按钮会纵向排列。
- 按钮没有 `white-space: nowrap`，长文案可以自然换行。

Gap: 未发现需要立即修改 CSS 的证据；后续若真实设备或截图发现按钮文字溢出，再进入 `SIMPLIFY` 收敛按钮文案或布局。

2026-05-07 阶段切换基线：3-5 分钟参与度。这不是真实玩家反馈。

- 触发：用户确认进入下一步。
- 阶段定义：前 60 秒清晰度不再是唯一主目标，转为回归护栏；新主目标是 3-5 分钟内的目标清晰度、升级节奏和回访理解。
- 已有证据：Issue #1 和 Issue #2 均已发布最小修复并回复一次，后续无新补充；60-300 秒模拟仍持续出现升级事件。
- 当前约束：不因阶段切换直接新增第三种升级、第二资源、prestige、任务系统、新面板、图片资产或 telemetry 上传。
- 新检查重点：第 180 秒和第 300 秒时，玩家是否知道下一步是在扩建自动采集器、调校效率，还是等待离线收益。
- 回归护栏：第 10 秒第一台自动采集器、第 23 秒第二台自动采集器、第 39 秒第三台自动采集器的早期节奏不能被新切片破坏。

Gap: 阶段已切换，但本轮不实现新玩法。下一步若要行动，必须基于新的 3-5 分钟 self-playtest gap、local-only metrics、真实反馈或 research-backed decision。

2026-05-07 3-15 分钟阶段预告检查。这不是真实玩家反馈。

- 输入状态：工坊阶段、下一升级进度和阶段完成反馈已实现；Issue #1/#2 无新玩家补充，本轮不重复回复 issue。
- 第 0-60 秒：下一阶段只从“拥有 3 台自动采集器”扩展为“拥有 3 台自动采集器，开启星尘小间”，不改变早期经济和按钮结构。
- 第 3-15 分钟：星尘小间和稳定工坊的下一阶段条件现在同时说明会进入“稳定工坊”或“星尘引擎室”，玩家能提前知道当前投入会推向哪个内容层。
- 复杂度复核：仍为 1 个主资源、0 个第二资源、2 种升级类型、同一主屏阶段行；没有新增面板、存档字段、指标字段或反馈渠道。

Gap: 已用延后解锁文案处理。本轮不新增第三或第四种升级；下一步仍必须来自新反馈、local-only metrics、research-backed decision 或新的 self-playtest gap。

## 检查清单

### 前 10 秒

- 玩家是否知道要点击什么？
- 资源增长是否可见？
- 是否有即时反馈？

### 前 30 秒

- 玩家是否能到达或理解第一个升级？
- 下一个目标是否可见？

### 前 60 秒

- 玩家是否理解核心循环？
- 是否有继续游玩的理由？
- UI 复杂度是否仍在预算内？

### 前 3-5 分钟

- 玩家是否仍能看懂下一次升级目标？
- 自动采集器数量和调校工具效率之间的选择是否清楚？
- 回访/离线收益是否能解释“离开后进度仍在继续”？
- 下一步是否可以通过现有主屏表达，而不是新增面板或第二资源？

## 下一候选改进

在添加新资源系统之前，优先处理真实反馈或 3-5 分钟 self-playtest 指向的目标清晰度、升级节奏或回访理解问题。Issue #1 和 Issue #2 已有最小修复和一次回复；除非出现新信息，不要继续基于同一 issue 改 UI。若继续 self-playtest，优先寻找可以删除、合并或收敛的信息，而不是新增信息；不要在首屏继续解释反馈表单，也不要无信号调整经济数值。调校工具发布后，后续重点观察玩家是否理解“效率升级会推迟下一台自动采集器”，不要提前用更多按钮或资源解决这个尚未验证的问题。当前首屏文案仍在预算内，CSS 已有窄屏纵向按钮布局；后续如要改 UI，应基于真实截图、设备检查或玩家反馈。
